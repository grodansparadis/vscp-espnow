
#include <stdio.h>
#include <stdlib.h>

//#define ESP_MALLOC(s)   malloc(s)
//#define ESP_CALLOC(1,s)   calloc(s,1)
//#define VSCP_REMALLOC(s) remalloc(s)
//#define ESP_FREE(x)     free(x)

// #define MIN(X, Y) (((X) < (Y)) ? (X) : (Y))
// #define MAX(X, Y) (((X) > (Y)) ? (X) : (Y))